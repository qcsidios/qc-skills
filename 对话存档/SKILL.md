---
name: 对话存档
description: 对话存档工具。每次对话结束时必须主动检查是否需要存档——不要等青城说"归档"，只要产生了新的决策、认知或产出就应存档。青城说"对话存档""归档""存一下"时立即触发。将对话按两层格式（AI摘要+主题式对话记录）写入 00-对话存档/对话记录/。存档前先更新知识卡片调用记录（count +1），ls 当天文件夹有则追加无则新建。6点后启动时 hook 自检触发整理修订，幂等标记 frontmatter `consolidated: true`。
followed_by: [日报]
---

# 对话存档

每场对话结束时，将对话内容按约定格式存档，确保后续 AI 能快速扫描历史对话。

## 为什么是这个流程

对话存档的本质是记录青城的思考演进和协作过程，不是操作日志。写作时问自己：**未来的青城回看这段记录，能看到自己当时的想法和决策路径吗？**

**「日」以早上 6 点为界**：6 点之前的对话属于前一天（含凌晨/熬夜），6 点之后属于新一天。这是与日报 Skill 共用的时间约定。

---

## 存档流程

### 第零步：更新调用记录（与归档同步）

存档前先更新调用记录。扫描本轮对话中 AI 显式引用过的知识卡片，在 `00-对话存档/知识卡片调用记录.md` 中将对应卡片的 count +1，更新后按降序排列。

判断标准：这张卡片的名称出现在了本次对话的思考或回复中 → 就算用过。

### 第负零点五步：归档前置检查（硬性前置）

**任何归档/追加操作前，必须先 `ls` 当天文件夹**：

- 有当天文件 → 用 `edit` 工具追加到末尾
- 无当天文件 → 用 `write_to_file` 工具新建

**「日」以早上 6 点为界**。即使过了零点，只要还没到 6 点，就还属于昨天。6 点之后第一次启动 Claude Code 时才进入新一天。

这条已被违反 3 次，写进 CLAUDE.md 根宪法行为铁律。每次归档前先停一下，问：今天的文件存在吗？

### 第一步：写新建文件头部（仅新建文件时）

新建文件以「H1 标题 + 一句话总览 + AI 摘要」三段起头。一句话总览不是可选项——根目录 `轨迹看板.md` 的 dataviewjs 优先抓这段渲染卡片摘要，缺失则降级到"主题"字段（短而标签化）。文件名碰巧符合正则不够，正文里必须真有这一句话。

完整骨架：

```markdown
# YYYY-MM-DD 主题关键词

> 一句话总览：表面 N 件事，骨子里在做 X。

---

## AI 摘要

- **主题**：[一句话概括]
- **青城状态**：[当前状态变化，无变化可写「稳定」]
- **关键决策**：[编号列表，每条一句话]
- **产出**：[具体产出物]
- **新认知**：[关于青城的新认知或偏好变化]
- **核心引用卡片**：[[卡1]]、[[卡2]]（本次对话中作为判断核心依据的知识卡片，3-5 张。周复盘定性补充依赖此字段）
- **待办**：[明确的后续动作，无则写「无」]
```

**一句话总览的写法**（80 字内）：
- 抓"骨子里在表达什么"——表面 N 件事，骨子里其实是一件事
- 不要复述"主题"字段——那一行已经说过一遍
- 一天只做了一件事时直接写"核心只有一句——XXX"
- 参考 5/20-5/24 已修订存档作为范例

**AI 摘要要点**：简洁，每条一行；关键决策编号，不展开；不写「可能」「或许」——只写确定的。

追加到已有文件时**不再写新的 H1 / 总览 / AI 摘要**——后面统一在「整理修订」步骤合并/重写。

### 第二步：追加 vs 新建

**追加到已有文件**：
- 用 `---` 分隔线
- 加新主题的二级标题（如 `## 主题 N：XXX`），**不用「七、八、九」继续编号**
- 内容紧接其下

**新建文件**：
- 文件名：`YYYY-MM-DD-[主题关键词].md`
- 写完头部三段（H1 + 一句话总览 + AI 摘要）后开始正文

### 第三步：写对话记录（按主题模块组织）

**核心原则：记录的是思考，不是操作。**

#### 写作要求

- **以青城的思考为主线**：他在想什么、想干什么、意图是什么、想法怎么演变的
- **写决策路径，不写操作清单**：不是「青城说了 X → 知白做了 Y」，而是「青城想要达到什么效果、为什么选择了这个方案、中间遇到了什么问题、怎么解决的」
- **捕捉协作模式**：他是怎么用我的、我哪里做得好哪里犯了错、他对我的行为做了什么修正
- **按主题合并，不按时间线分节**：同一主题的多个来回归入同一节

#### 写法示范

- ✅ 「青城对归档频率经历了完整的探索：10min→30min→1h→整点→最终30min。背后是一个发现过程：太频繁没必要，整点有盲区（对话中 cron 不触发）。整个过程体现了他对系统设计的敏感——不是随便调参数，而是在找平衡点。」
- ❌ 「青城将自动存档定时从 10 分钟改为 30 分钟。知白删除旧 cron 任务，新建 30 分钟 durable cron。」

#### 不保留的

- 纯执行确认和工具调用过程
- 重复操作细节
- 没有新认知或决策的闲聊

### 第四步：信息回流

存档完成后检查：
- 对话中是否产生了应进入知识库的新认知？→ 编译为原子卡片写入 `02-知识库/`
- 对话中是否更新了对青城的认知？→ 更新 `01-关于我/` 对应文件
- 回流时遵循正常编译流程，不因「对话刚结束」而跳过深度处理

判断标准：回流的是知识/认知，不是对话本身。对话存档是原始记录，知识库是编译后的成果。两者互补不重复。

---

## 整理修订（一天结束时的精简动作）

当一天的对话存档因为多次追加变得碎片化时，做整理修订让它变成可读的整体。

### 触发时机

**强制触发（必跑）**：
- **每日最后一次归档时**：青城说睡了 / 明确表示当天结束
- **6 点之后启动 Claude Code 时由 hook 自检 + AI 直接执行**：hook 检查昨日对话存档是否带 frontmatter `consolidated: true`，缺标记则注入"待执行"指令，AI 收到就**直接整理修订并补标记**，不要询问青城是否触发；不要把"今天开了第几个终端"当判断依据——只看标记。已有标记则 hook 静默，AI 不要主动跑

**幂等机制**：整理修订完成后必须在文件 frontmatter 写入 `consolidated: true` 和 `consolidated_at: YYYY-MM-DD`。这是下次 hook 判断是否需要再跑的唯一依据。

**条件触发**：
- 青城说「整理一下」「重新整理这天的存档」
- 当天文件被追加 3 次以上
- 章节编号出现重复（如同一文件里两个「一、二、三」）
- 反思在多处重复（如同一错误的反思在 3 处分别写过）

**触发后必做**：① 重新组织主题 ② 合并冗余 ③ 提炼跨主题隐线 ④ **同步修改文件名**让标题反映全天内容（而不是只反映上午或某个阶段的事件）

### 整理动作

1. **重命名文件**：让标题反映全天内容，而不是只反映上午（如「日报改名」→「基础设施重构与知识库 MOC 化」）
2. **用主题模块组织**：`## 主题 1：XXX` / `## 主题 2：XXX`，**不用「一二三四五六」硬章节号**
3. **合并重复反思**：同一规则的反复失败 → 表格 + 元层级反思
4. **提炼跨主题隐线**：找出当天所有主题在表达的「同一件事」，作为最后一段
5. **合并产出清单**：按类别分组（协议/结构/工具链/Skill/知识库/个人说明书），不按时段切
6. **写入幂等标记（不可省）**：整理修订完成后，在文件顶部加 frontmatter `consolidated: true` 和 `consolidated_at: YYYY-MM-DD`。漏写这一步 = 下次启动 hook 还会再报"未整理"，等于工作做了一半。
7. **行数检查（整理后独立执行）**：整理修订完成后，检查文件行数。超过 400 行 → 触发二次浓缩（合并相似主题、删除已被知识库承接的重复认知、把过程性来回归并成结论）。未超 400 行 → 跳过压缩，不额外操作。**整理和压缩是两个独立步骤，不要混在一起做。**

### 整理后的文件骨架

```markdown
---
consolidated: true
consolidated_at: YYYY-MM-DD
---

# YYYY-MM-DD 主题概括

> 一句话总览：表面 N 件事，骨子里在做 X。

---

## 主题 1：[名称]
### [子主题 1.1]
### [子主题 1.2]

## 主题 2：[名称]
### [子主题 2.1]

## 主题 N：...

---

## 贯穿全天的反思

### 同一规则的反复失败（用表格合并多次错误）
| 时段 | 错误 | 我的反应 |
|------|------|----------|
| 上午 | ... | 修正 + 规则确立 |
| 下午 | ... | 修正 + 规则升级 |
| 凌晨 | ... | 修正 + 升级到根宪法 |

### 设计层面的错误
### 小错误集合

---

## 全天产出清单

### 协议与规则
### 文件结构
### 工具链
### 知识库
### 个人说明书

---

## 这一天确立的原则

跨主题汇总，提炼成 5-10 条编号原则。

---

## 跨主题的隐线

回看这一天，所有 N 个主题在表达**同一件事**：[一句话洞察]

- 主题 1 是它的子表达
- 主题 2 是它的子表达
- ...

[补充段落]
```

### 完整范本

参见 `00-对话存档/对话记录/2026-05-20-基础设施重构与知识库MOC化.md`——按这个结构整理的实际范例。

---

## 铁律

- **归档前必须 ls 当天文件**：硬性前置，违反会进根宪法记录
- **新建文件必含一句话总览**：轨迹看板 dataviewjs 第一优先级抓这段；缺失则降级渲染，破坏看板一致性
- **对话结束即存档**：不拖延，不等青城提醒
- **不写逐字稿**：总结型叙述，保留判断不保留闲聊
- **按主题归并，不按时间分节**：同一主题的多个来回归入同一节
- **主题模块化不用硬章节号**：用 `## 主题 N：XXX` 而非「一二三四五六」
- **整理修订幂等触发**：每日最后一次归档时 OR 6 点后启动 hook 注入"待执行"指令时，AI **直接执行**——不要询问青城。完成必写 frontmatter `consolidated: true` 标记，缺标记 = 未完成
- **跨主题隐线必提炼**：找出当天所有主题在表达的同一件事
- **信息不重复沉淀**：对话存档 + 知识库/说明书各司其职，同步回流但不复制粘贴
- **文件名中文优先**
